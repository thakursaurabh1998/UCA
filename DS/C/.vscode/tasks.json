{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build compile",
            "type": "shell",
            "command": "gcc \"${file}\" && ./a.exe"
        },
        {
            "label": "build compile only",
            "type": "shell",
            "command": "gcc -g \"${file}\"",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}